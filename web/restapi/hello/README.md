# RESTfull APIs in C++

This is an example take from [Building RESTfull APIs with C++](https://medium.com/@AlexanderObregon/building-restful-apis-with-c-4c8ac63fe8a7).

This example doesn't work because when the `ioc` (`io_context`) fails.
