#include <iostream>
#include <cstdlib>

int
main(int argc, char *argv[]) {

  int nb, nm;
  char tm;

  if (argc == 0) {
    while (std::cin >> nb >> tm >> nm) {
    
    }
  }
  else

  return EXIT_SUCCESS;
}
